server:
    error:
      whitelabel:
        enabled: true
    
eureka:
  client:
    serviceUrl:
        defaultZone: http://nextgen-registry:8088/eureka/
          
logging:
  level:
     org:
       springframework:
         security: DEBUG  

hystrix:
  metrics:
    enabled: true         
